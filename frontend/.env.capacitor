# Capacitor（Android APP）专用构建环境
#
# 用法：npx vite build --mode capacitor
#
# 与 .env.production 的区别，两处都是必须的：

# 1) 接口地址用**绝对 URL**。
#    网页版靠 '/prod-api' 这种相对路径 + 服务器反代，前提是「同源」。
#    APP 里前端资源是本地打包的，没有同源可言，相对路径会打到手机自己身上。
#    写死成绝对地址，就不依赖运行时探测 Capacitor 环境了（探测失败会退化，
#    而退化后的现象是「全都连不上」，很难定位）。
#    APP 内仍可在「服务器设置」里改成别的地址，这里只是默认值。
VITE_APP_TITLE = 田诊助手
VITE_APP_ENV = 'capacitor'
VITE_APP_BASE_API = 'https://revenge-duh-attest.ngrok-free.dev/dev-api'

# 2) **不设 VITE_BUILD_COMPRESS**。
#    设了会额外产出 .gz 文件，那是给 nginx 做预压缩用的，APK 里毫无意义；
#    更麻烦的是 cap sync 会把 .gz 和原文件一起复制进 Android 资源目录，
#    Android 的资源合并器会因此报 "Duplicate resources" 直接构建失败。
