# 本机口令模板
#
# 用法：把这个文件复制成同目录下的 .secrets.local，填入真实口令。
#       .secrets.local 已被 .gitignore 排除，不会进仓库。
#
#     copy scripts\.secrets.local.example scripts\.secrets.local
#
# 这些值供三个脚本读取（也可以直接用环境变量覆盖，环境变量优先）：
#   TZ_PASSWORD       admin 账号口令 —— test-api.py（接口回归）、seed-demo.py（演示数据）
#   TZ_DB_PASSWORD    本机 MySQL 口令 —— check-env.ps1（环境自检）
#
TZ_PASSWORD=CHANGE_ME_ADMIN_PASSWORD
TZ_DB_PASSWORD=CHANGE_ME_DB_PASSWORD
